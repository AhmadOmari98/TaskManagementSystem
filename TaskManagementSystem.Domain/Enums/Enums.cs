
namespace TaskManagementSystem.Domain.Enums
{
    public enum UserRole
    {
        Admin = 1,
        User = 2
    }

    public enum WorkItemStatus
    {
        New = 1,
        Pending = 2,
        InProgress = 3,
        Completed = 4
    }
}
